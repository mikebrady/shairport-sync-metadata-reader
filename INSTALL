Installation Instructions
*************************
Make the shairport-sync-metadata-reader directory the default and perform the following commands:

$autoreconf -f -i
$./configure
$make

That's it.
